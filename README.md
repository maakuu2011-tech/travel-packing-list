# 旅じたくリスト

旅行条件を選ぶと、必要な持ち物を整理できる日本語のチェックリストサイトです。

## 開発

```powershell
npm install
npm run dev
```

## ビルド

```powershell
npm run build
```

本番URLは `PUBLIC_SITE_URL` で指定します。未指定時は仮の Cloudflare Pages URLを使用します。

